<% flash.each_with_index do |(message_type, message), index| %>
  <div class="flash flash-<%= message_type %> text-xl message" style="top: <%= 85 + (index *50) %>px;">
    <%= message %>
  </div>
<% end %>
<div class="load-message load text-xl" style="top: 85px; display: none;">
  読み込み中…
</div>
